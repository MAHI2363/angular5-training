import { HeightAdjusterDirective } from './height-adjuster.directive';

describe('HeightAdjusterDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightAdjusterDirective();
    expect(directive).toBeTruthy();
  });
});
